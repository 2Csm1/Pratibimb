# requirements-dev.txt — Development-only dependencies
# Install with: pip install -r requirements-dev.txt
# These are NOT needed to run the app in production.

-r requirements.txt

pytest==7.4.0
pytest-flask==1.3.0
jupyter==1.0.0
tqdm==4.65.0
